# Shared secret gating this app -- there is no per-founder login, just one
# passphrase known to the founders. Rotate it if it ever leaks.
FOUNDER_PASSCODE=change-me

# MIRA backend base URL (same one the host dashboard uses), no trailing slash.
BACKEND_BASE_URL=http://localhost:8000
